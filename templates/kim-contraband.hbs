<div class="cv-kim cv-kim-drop">

  <div class="cv-kim-drop-head">
    <span class="cv-kim-drop-ico">📦</span>
    <div class="cv-kim-drop-id">
      <span class="cv-kim-drop-title">DEAD DROP</span>
      <span class="cv-kim-drop-sub">HQ Contraband Manifest</span>
    </div>
    <span class="cv-kim-drop-stock {{#unless hqStock}}cv-kim-drop-empty{{/unless}}">HQ {{hqStock}}/{{hqCap}}</span>
  </div>

  {{#if canWrite}}
  <div class="cv-kim-drop-form">
    <label>{{localize 'cain-cardinal-virtuoso.drop.recipient'}}</label>
    <select name="dropVirtue">
      {{#each dropRecipients}}
      <option value="{{key}}" {{#if selected}}selected{{/if}}>{{name}} — {{status}} ({{used}}/{{../cap}})</option>
      {{else}}
      <option value="" disabled selected>{{localize 'cain-cardinal-virtuoso.drop.noBondRecipient'}}</option>
      {{/each}}
    </select>

    <label>{{localize 'cain-cardinal-virtuoso.drop.package'}}</label>
    {{#if hasItems}}
    <select name="dropItem">
      {{#each items}}
      <option value="{{itemId}}">{{name}} ×{{qty}}</option>
      {{/each}}
    </select>
    <button type="button" class="cv-btn cv-btn-on" data-action="drop">{{localize 'cain-cardinal-virtuoso.drop.send'}} &#9656;</button>
    {{else}}
    <div class="cv-kim-empty">{{localize 'cain-cardinal-virtuoso.drop.noItems'}}</div>
    {{/if}}
  </div>
  <div class="cv-kim-drop-hint">{{localize 'cain-cardinal-virtuoso.drop.hint'}}</div>

  {{#if hasPending}}
  <div class="cv-kim-drop-pending">
    <div class="cv-kim-group">{{localize 'cain-cardinal-virtuoso.drop.pending'}}</div>
    <ul class="cv-kim-notes">
      {{#each pending}}<li>{{glyph}} {{item}} &#9656; {{name}}</li>{{/each}}
    </ul>
  </div>
  {{/if}}

  <div class="cv-kim-drop-gifts">
    <div class="cv-kim-group">{{localize 'cain-cardinal-virtuoso.gift.heading'}}</div>
    {{#if hasGifts}}
    <label>{{localize 'cain-cardinal-virtuoso.drop.recipient'}}</label>
    <select name="giftVirtue">
      {{#each giftRecipients}}
      <option value="{{key}}">{{name}} — {{status}}</option>
      {{else}}
      <option value="" disabled selected>{{localize 'cain-cardinal-virtuoso.gift.noRecipient'}}</option>
      {{/each}}
    </select>

    <label>{{localize 'cain-cardinal-virtuoso.gift.item'}}</label>
    <select name="giftItem">
      {{#each gifts}}
      <option value="{{itemId}}" title="{{desc}}">{{glyph}} {{name}} ×{{qty}}</option>
      {{/each}}
    </select>

    <label class="cv-kim-drop-fresh"><input type="checkbox" name="giftFresh" /> {{localize 'cain-cardinal-virtuoso.gift.fresh'}}</label>

    <button type="button" class="cv-btn cv-btn-on" data-action="dropGift">{{localize 'cain-cardinal-virtuoso.gift.send'}} &#9656;</button>
    <div class="cv-kim-drop-hint">{{localize 'cain-cardinal-virtuoso.gift.hint'}}</div>
    {{else}}
    <div class="cv-kim-empty">{{localize 'cain-cardinal-virtuoso.gift.none'}}</div>
    {{/if}}
  </div>
  {{else}}
  <div class="cv-kim-drop-hint">{{localize 'cain-cardinal-virtuoso.drop.noClearance'}}</div>
  {{/if}}

  <div class="cv-kim-drop-log">
    <div class="cv-kim-group">{{localize 'cain-cardinal-virtuoso.drop.recent'}}</div>
    <ul class="cv-kim-notes">
      {{#each drops}}<li>{{this}}</li>{{else}}<li class="cv-kim-empty">{{localize 'cain-cardinal-virtuoso.drop.noDrops'}}</li>{{/each}}
    </ul>
  </div>

</div>
